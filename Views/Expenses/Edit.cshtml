@page
@model budge.Views.Expenses.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Expenses</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Expenses.ID" />
            <div class="form-group">
                <label asp-for="Expenses.Label" class="control-label"></label>
                <input asp-for="Expenses.Label" class="form-control" />
                <span asp-validation-for="Expenses.Label" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Expenses.Amount" class="control-label"></label>
                <input asp-for="Expenses.Amount" class="form-control" />
                <span asp-validation-for="Expenses.Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
